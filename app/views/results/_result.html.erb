<%- model_class = Result -%>
<%= render :partial => "results/form", :locals => { :task => task } %>
<br />
<table class="table table-striped table-condensed">
  <thead>
    <tr>
      <th>Results</th>
    </tr>
  </thead>
  <tbody>
    <% task.results.delete_if(&:new_record?).each do |result| %>
    <tr>
      <td>
	<ul class="nav nav-tabs" id="resulttabs" data-tabs="tabs">
          <li class="active"><a href="#detailsresult<%= result.id %>"
				data-toggle="tab">Details</a></li>
	  <li><a href="#editresult<%= result.id %>" data-toggle="tab">Edit</a></li>
          <li><a href="#historyresult<%= result.id %>" data-toggle="tab">History</a></li>
	</ul>
	<div class="tab-content">
          <div class="tab-pane active" id="detailsresult<%= result.id %>">
	    <%= result.description %><br />
	    <%= link_to result.document.identifier, result.document.url %><br />
	  </div>
	  <div class="tab-pane" id="editresult<%= result.id %>">
	    <%= render :partial => "results/editform", :locals => { :result =>
	    result, :task => task } %>
          </div>
	  <div class="tab-pane" id="historyresult<%= result.id %>">
            <%= render :partial => "results/history", :locals => { :result => result } %>
	  </div>
	</div>

      </td>
    </tr>
<% end %>
</tbody>
</table>

